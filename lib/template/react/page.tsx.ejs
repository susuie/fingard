import { FC } from 'react';
import Page from '@/components/PageContent/pageContent';
import { message } from 'antd';


const <%= data.name %>: FC = () => {
    return <Page pageCode="<%= data.pageCode %>"></Page>;
    };

    export default <%= data.name %>;